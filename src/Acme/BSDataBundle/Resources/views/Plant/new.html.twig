<h1>Plant creation</h1>

<form action="{{ path('BSData_plant_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('BSData_plant') }}">
            Back to the list
        </a>
    </li>
</ul>
