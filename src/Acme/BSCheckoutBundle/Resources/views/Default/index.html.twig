
{% extends '::base2.html.twig' %}

{% block title %}Kasse{% endblock %}

{% block javascripts %}
    <script src="{{ asset('/js/jquery.js') }}" type="text/javascript"></script>
    <script src="{{ asset('/js/checkout.js') }}" type="text/javascript"></script>
{% endblock %}

{% block body %}
    <h1>Kasse </h1>
    </div>
    </header>
    <div class="ym-wrapper">

        <div class="ym-column">
            <div class="ym-col1">

                <div class="ym-cbox">
                    <div class="shopinglist">
                        <table >
                            <thead>

                            <tr>
                                <th>Code</th>
                                <th>Menge</th>
                                <th>Bezeichung</th>
                                <th>Preis</th>
                                <th>PA</th>
                                <th>Summe</th>
                            </tr>
                            </thead>
                            <tbody class="co_items">
                            {% for item in basket.checkoutItems %}
                                <tr data-sum="{{ (item.quantity * item.price) }}">
                                    <td>{{ item.articleCode }}</td>
                                    <td>{{ item.quantity }}</td>
                                    <td>{{ item.description }}</td>
                                    <td>{{ item.price|number_format(2, '.', ',')  }} &euro;</td>
                                    <td> </td>
                                    <td>{{ (item.quantity * item.price)|number_format(2, '.', ',')  }}  &euro;</td>

                                </tr>


                            {% endfor %}
                            </tbody>
                        </table>

                    </div>

                    <input type="text" class="inputkeyboard">


                    <a class="btn" href=" {{ path('BSCheckout_clear', { 'cashbox_id': 1 }) }}"> CLEAR</a>

                </div>
            </div>

            <div class="ym-col2">
                <div class="ym-cbox">

                    <div class="checkout-box">
                    </div>
                    <div class="checkout-box">
                    </div>
                    <div class="checkout-box">
                    </div>
                    <div class="checkout-box">
                    </div>
                    <div class="checkout-box">
                    </div>
                    <div class="checkout-box">
                    </div>
                    <div class="checkout-box">
                    </div>
                    <div class="checkout-box">
                    </div>
                    <div class="checkout-box">
                    </div>

                    <div class="co_result">
                        Betrag
                        <div class="co_toPay">

                        </div>
                        Bezahlt
                        <input type="text" class="co_payed" >
                        Rückgeld
                        <div class="co_return">

                        </div>

                    </div>





                </div>
            </div>
        </div>

    </div>

{% endblock %}