<h1>quickbutton list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Code</th>
            <th>Price</th>
            <th>Quickkey</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('cashbox_quickbutton_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.name }}</td>
            <td>{{ entity.code }}</td>
            <td>{{ entity.price }}</td>
            <td>{{ entity.quickkey }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('cashbox_quickbutton_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cashbox_quickbutton_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('cashbox_quickbutton_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
