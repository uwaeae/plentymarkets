<h1>quickbutton creation</h1>

<form action="{{ path('cashbox_quickbutton_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('cashbox_quickbutton') }}">
            Back to the list
        </a>
    </li>
</ul>
